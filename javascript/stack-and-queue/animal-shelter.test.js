'use strict';

const { Animal, AnimalShelter } = require('./animal-shelter.js');

describe('Animal Shelter', () => {
	test('can successfully add an animal to the queue', () => {
		const q = n
	})
});
